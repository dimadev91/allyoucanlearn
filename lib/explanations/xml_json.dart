const String exXmlJson =
    'Usually the API provides the same data either in \n'
    'XML or JSON format, and we can choose between the two.';

const String exXml =
    'XML is essentially a bunch of key-value pairs.'
    'The name of the key is the tag, there\'s an opening\n'
    'tag and a closing tag and a value fo the key in\n'
    'between as follows:\n'
    '<key>value</key>';

const String exJson =
    'JSON stands for JavaScript Object Notation.\n'
    'so it\'s very similar of how you create objects\n'
    'in JavaScript. And it\'s styled like this\n'
    '{key: value}';
